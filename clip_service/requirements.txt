# CPU-only torch is installed separately in the Dockerfile to pull the
# smaller wheel (~200MB vs ~2GB for CUDA). Do not add torch here.
open-clip-torch>=2.24.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
Pillow>=10.3.0
httpx>=0.27.0
pydantic>=2.7.0

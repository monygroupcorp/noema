/**
 * Platform Common Module
 * 
 * Exports shared platform abstraction interfaces and utilities.
 */

const PlatformAdapter = require('./PlatformAdapter');

module.exports = {
  PlatformAdapter
};


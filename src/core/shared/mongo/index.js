/**
 * MongoDB Repository Module
 * Exports MongoDB repository classes and utilities
 * 
 * @module core/shared/mongo
 */

const { MongoRepository } = require('./MongoRepository');

module.exports = {
  MongoRepository,
  // Export any additional MongoDB utilities here
}; 
/**
 * Workflow Migrations Module
 *
 * Exports services for migrating spell configurations when tool schemas change.
 */

const SpellMigrator = require('./SpellMigrator');

module.exports = {
  SpellMigrator
};

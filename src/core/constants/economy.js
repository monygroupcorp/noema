/**
 * Economy-related constants
 * Centralized location for all economy and financial constants
 */

// Point to USD conversion rate (1 point = $0.000337 USD)
const USD_PER_POINT = 0.000337;

// Default agent/collection owner rev-share in basis points (500 = 5%)
const DEFAULT_AGENT_OWNER_REV_SHARE_BPS = 500;

module.exports = {
  USD_PER_POINT,
  DEFAULT_AGENT_OWNER_REV_SHARE_BPS,
};


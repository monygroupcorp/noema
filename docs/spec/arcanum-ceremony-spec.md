# Arcanum Ceremony — Build Spec

**Status:** Phase 0 complete — dev ceremony done, verifier wired  
**Branch:** chainengine-migration  
**Goal:** ZK anonymous credit, fully proven end-to-end. Dev ceremony now. Public ceremony at launch.

---

## System map

```
[note] → client prover (snarkjs WASM) → ArcanumSpendProof
                                              ↓
                                    POST /v1/runs {arcanumProof}
                                              ↓
                                   ActumInceptor → ArcanumVerifier
                                   (Groth16 verify + nullifier check)
                                              ↓
                                         run executes
```

**Identity modes:**
- `{animaId}` — identified (bot, API key)
- `{commitment}` — legacy anon shortcut (seeded signum, no ZK)
- `{arcanumProof}` — REAL anon (ZK proof, unlinkable) ← proving this

---

## Artifacts (one-time, committed)

| File | Size | Location | Status |
|---|---|---|---|
| `arcanum.r1cs` | 3.9MB | `src/arcanum/circuit/artifacts/` | ✅ compiled (gitignore, not committed) |
| `arcanum.wasm` | 2.0MB | same | ✅ compiled (gitignore; serve via endpoint) |
| `arcanum_final.zkey` | 5.0MB | R2 / CDN (not git) | ✅ dev setup done (gitignore; upload to R2 for clients) |
| `verification_key.json` | 3.4KB | `src/arcanum/circuit/artifacts/` | ✅ committed — loaded by index.ts at startup |

---

## Phases

### Phase 0 — Dev ceremony ✅ DONE (2026-06-12)
Solo trusted setup run. Circuit: 8470 constraints, depth 32.  
Contribution hash: `3cd6149e 514c81c7 894ea314 53cffbd2 ...`  
`verification_key.json` committed. `.zkey` / `.ptau` gitignored (large, host on R2).  
ptau source (old hermez S3 is dead): `https://storage.googleapis.com/zkevm/ptau/powersOfTau28_hez_final_20.ptau`

### Phase 1 — Wire verifier ✅ DONE (2026-06-12)
`src/index.ts` now loads `verification_key.json` via `existsSync` + `readFileSync` at startup.  
Passes `arcanumVerifyFn: makeSnarkjsVerifier(vKey)` to `createContainer` when file present.  
Logs `ZK spend proofs active` or `ZK proofs disabled` on startup.

### Phase 2 — Client prover SDK ✅ DONE (2026-06-12) (`src/arcanum/prover.ts`)
```ts
generateSpendProof(
  note: ArcanumNote,
  merkleProof: ArcanumMerkleProof,
  recipient: string,              // hash(modoId, aditusId) — binds to execution
  opts: { wasmPath: string, zkeyPath: string }
) → Promise<ArcanumSpendProof>
```
Wraps `snarkjs.groth16.fullProve()`. Input shape:
```ts
{ nullifier, secret, pathElements[32], pathIndices[32],  // private
  root, nullifierHash, valor, recipient }                 // public
```

### Phase 3 — Serve circuit assets ✅ DONE (2026-06-12)
- `GET /arcanum/circuit/wasm` → streams `arcanum.wasm` (2MB, immutable cache header)
- `GET /arcanum/config` → `{ wasmUrl, zkeyUrl, depth:32, ready }` (discovery)
- `POST /v1/runs/quote` now returns `{ impetus, recipient }` — clients use `recipient` when generating proof
- `ARCANUM_ZKEY_URL` env var → passed into `/arcanum/config` response for client to know where to fetch .zkey
- Arcanum router now mounted at `/arcanum` in `index.ts` (was wired but never mounted)

### Phase 4 — Real ZK proof verified ✅ DONE (2026-06-12)
- `generateSpendProof()` + `ArcanumVerifier.verify()` with real wasm+zkey: **PASSES** (~970ms in Node)
- Tampered proof correctly rejected
- `tests/unit/arcanum/ArcanumProver.real.test.ts` — 2 real Groth16 tests, both green
- `computeRecipient` in prover.ts verified to match `ActumInceptor._computeRecipient` exactly
- **Next: staging deploy + live API verify** (issue note → generateSpendProof → POST /v1/runs {arcanumProof})

### Phase 5 — Blind issuance (spec complete, `docs/arcanum-blind-issuance.md`)
CreditVault UUPS upgrade → `payETHAnonymous(bytes32 commitment)` → no animaId ever created.  
Backend: `AnonymousDeposit` event → `arcanumTree.insert(commitment, valor)`.  
New endpoint: `GET /arcanum/tree/leaf/:commitment` (poll after deposit).  
**Dependency:** Phase 0–4 proven first.

### Phase 6 — Public ceremony frontend
In-browser snarkjs ceremony UI. Route: `/ceremony` or standalone page.

**Coordinator flow:**
1. Upload `arcanum_0000.zkey` (pre-generated by coordinator CLI)
2. Set participant list, share link

**Contributor flow (in-browser, no CLI):**
1. Download prev zkey from coordinator server (or direct link)
2. `snarkjs.zKey.contribute(prevZkey, entropy)` — runs in-browser WASM
3. Download their contribution zkey + contribution hash shown
4. Upload contribution back to coordinator server
5. Attestation pre-filled, copy to paste

**Coordinator finalize:**
1. Server receives all contributions in order
2. Apply random beacon (`snarkjs.zKey.beacon`)
3. Export `verification_key.json`
4. Publish transcript (hash chain + attestations)

**Backend (shipped, `ceremoniaRouter.ts`, mounted `/v1/ceremony` at `mountCeremony.ts:70`):**
- `GET /v1/ceremony` — status (contributor queue, hashes) — this doc's "state"
- `POST /v1/ceremony/slots` — reserve a contribution slot
- `GET /v1/ceremony/current.zkey` — download the current zkey to contribute against
- `POST /v1/ceremony/contributions` — upload a contribution — this doc's "upload"
- Transcript page: static or server-rendered, shows hash chain

**Finalize is not a request-triggered endpoint.** The beacon + vkey export is a deploy-time
toggle, `CEREMONY_FINALIZE=<hash>`, applied at boot (`mountCeremony.ts:56-68`) and only when the
ceremony is in the `open` phase. A one-shot trusted-setup finalization that cannot be triggered by
an inbound request is the safer shape — there is no admin-auth surface to compromise.

**This is the launch hype moment.** Announce ceremony → invite community → each person clicks through in-browser → platform goes live with their randomness in it.

---

## Constraints / gotchas

- Circuit depth=32, permanent. Never rerun ceremony for a different circuit.
- `.zkey` is ~300MB — never git. Always R2/CDN.
- Proof generation is ~10-30s in WASM (browser), ~2s in Node. Warn users.
- `recipient = poseidon(modoId, aditusId)` — proof is bound to one execution; can't replay.
- `commitment` shortcut (`x-commitment` header) stays for backward compat but is NOT unlinkable.
- After ceremony: old dev `verification_key.json` must be REPLACED. Any notes issued against the dev key cannot be spent against the production key (different verification key = different trust root).

---

## Open questions
- R2 bucket for `.zkey` — existing media bucket or new `arcanum` bucket?
- Ceremony frontend: standalone HTML page (no framework) vs React component on main site?
- Public ceremony invite strategy: Telegram announcement? Twitter? Core team only first?

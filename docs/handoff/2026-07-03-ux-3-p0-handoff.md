# UX Handoff 3 · P0 — stop the bleeding

> **STATUS — worked 2026-07-06 (working tree, typecheck-green, browser-verified).** Triaged into
> wire-now vs. backend-blocked. Half the packets have no backend to complete against, so those were
> made *honest* rather than faked.
> - **P0-1 Funding** — DONE (honest). Onchain rail is the real path: "Connect" now wires to
>   `connectWallet()`, deposit address is copyable → send ETH → webhook credits. Card has **no Stripe
>   backend** → disabled "coming soon".
> - **P0-2 Dashboard CTA** — DONE. "Create something" → `/catalog` (+ "Ask the Concierge" → `/chat`).
> - **P0-3 Train→shelf** — PARTIAL. Real "View your shelf →" / `/models` link added + run marked a
>   preview. Full completion→shelf needs a **training-runs index (no backend)**.
> - **P0-4 Catalog honesty** — DONE. License/Size/Runs were id-hash **fabrications** → columns removed
>   (compliance-sensitive); table now shows Model · Modality · Version (real). Fake workflow count gone.
> - **P0-5 Author review** — DONE. Backend `/v1/editiones/review` is already dual-audience; added the
>   `/review` route + "In review" on the Publish pillar + Card links there. Author view verified.
> - **P0-6 Spend ledger** — BLOCKED. No settled-run/cost endpoint (`me.gens` is active-only); the
>   honest empty state (built in Handoff 2) is correct.
> - **P0-7 Account/GDPR** — DONE (honest). "Go anonymous" wired to `session.goAnonymous()`;
>   Export-everything + Delete-account have **no backend** → disabled "coming soon" + a legal-rights note.
> - **P0-8 Datasets front door** — BLOCKED → honest. There is **no dataset backend at all** (even
>   `listDatasets` calls an unserved route); both "new dataset" affordances disabled "coming soon".
> - **Onboarding doors / spend ledger** already honest from Handoffs 1–2; unchanged.
>
> **Backend tickets this surfaced (frontend can't close these):** card/Stripe funding · training-runs
> index · per-run spend ledger · account data-export + deletion (GDPR) · a datasets backend
> (create + list). Until those land, the surfaces are honestly "coming soon," not faked.



**Bucket:** P0 — a core job cannot complete, or the UI actively misleads. **Do this last.**
**Source:** `docs/plans/2026-07-03-ux-flow-audit.md`
**Why last:** these are real-user walls, but most are *wiring* — and they should land onto the settled
IA from Handoff 2, not into screens we're about to move. Each packet below notes its dependency on
Handoff 2. Where a P0 was *honesty-gated* in Handoff 1 (onboarding doors, mock buttons), this handoff
does the actual wiring.

Each packet: **problem → files → done when → depends on.**

---

## P0-1 · "Buy points" is impossible
The only working Funding button mints a mock Vault; Connect-wallet, Pay-with-card, and pack chips are
dead — the core monetization job can't complete.
- **Files:** `Funding.tsx:194` (Connect), `:249` (Pay with card), `:159-171` (pack chips).
- **Done when:** at least one purchase path (onchain deposit *or* card) completes end-to-end and the
  balance updates; unbuilt paths are explicitly "coming soon," not dead.
- **Depends on:** Handoff 2 Decision 2 (Funding stays under Account) — low risk, can start early.

## P0-2 · New user has nowhere to "make"
Dashboard has no generate CTA; the Rail's "Make" section (Chat, Canvas) is mock; the only live path
hides under Library → Catalogue → Card.
- **Files:** `Dashboard.tsx` (add a prominent "Create something" CTA → Catalogue→Card), `Rail.tsx:21-28`.
- **Done when:** a first-run user reaches a working generation in one obvious click from `/app`.
- **Depends on:** Handoff 2 Decision 1 (the "Create" pillar + Card's home). **Blocked until nav lands.**

## P0-3 · Train → model handoff is missing
TrainRun promises "lands on your shelf" but never links to `/models`; no training-runs index exists.
- **Files:** `TrainRun.tsx:94` (add completion state + "View on your shelf →" `/models`, ideally
  deep-linked to the model); add an **active/finished runs index** (Dataset hub + Models placeholders).
- **Done when:** finishing a training run leads to the model in ≤1 click, and an in-flight run is
  re-findable after refresh.
- **Depends on:** Handoff 2 Decision 5 (Projects/runs surfacing) for the index location.

## P0-4 · Catalog fabricates license / size / locality
Those columns come from an id hash — users choose models (incl. the compliance-sensitive **license**)
on fiction.
- **Files:** `Catalog.tsx:46-70`, `:94-96` (fabricated workflow count).
- **Done when:** columns show real API data or are hidden; nothing about license/size/locality is
  invented. **(Compliance-sensitive — prioritize within P0.)**
- **Depends on:** API carrying the fields (may need a backend ticket); until then, hide.

## P0-5 · Authors can't see their own review status
Card says "in review," but `/admin/review` (which has an author view) is linked only for `me.admin`.
- **Files:** `Review.tsx` (author view), `Rail.tsx:63`, `Card.tsx:298`.
- **Done when:** a non-admin author has a discoverable path to their held items; rename the `/admin/`
  path for the author-facing case.
- **Depends on:** Handoff 2 Decision 1 (Publish pillar → Review home).

## P0-6 · Spend history does not exist
Status shows only *active* runs — no ledger, no per-run cost, no spend-over-time.
- **Files:** build the ledger in the new **Activity** screen (`Status.tsx` → Activity).
- **Done when:** a user can see settled runs with per-run cost and a running total.
- **Depends on:** Handoff 2 Decision 2 (Activity screen) — **build them together.**

## P0-7 · Account settings is ~90% mock
Billing / api / security panes are placeholders; the load-bearing **Export everything** / **Delete
account** (GDPR) buttons have no handler.
- **Files:** `AccountSettings.tsx:72-75` (Export/Delete), `:127-132` (panes), `:80-99` (hardcoded data).
- **Done when:** Export + Delete actually work (legal requirement); panes show real data or honest
  "coming soon."
- **Depends on:** Handoff 2 Decision 2 (Settings consolidation) — **build them together.**

## P0-8 · Datasets front door is inert
Both "new dataset" affordances have no handler — the entry to the whole training stack does nothing.
- **Files:** `Datasets.tsx:20,45`.
- **Done when:** "new dataset" opens a real create flow.
- **Depends on:** nothing structural — can start early.

---

### Also land here (honesty-gated in Handoff 1, wired here)
- **Onboarding doors** (`Onboard.tsx:58,127`) — wire Wallet/Passkey/Bursa, or keep them "coming soon."
- **Compute posture** (`Card.tsx:144-146`) — either make TEE/local dispatch real, or keep the control
  visibly a preview (ties to Handoff 2 Decision 8).

### Fast-start (no architecture dependency)
P0-1 (Funding), P0-4 (Catalog honesty), P0-8 (Datasets front door) can begin immediately in parallel
with Handoff 1/2.

---

**Sequence:** [Polish](./2026-07-03-ux-1-polish-handoff.md) → [Architecture](./2026-07-03-ux-2-architecture-handoff.md) → P0 (you are here — final).
**← Prev:** [Handoff 2 · Architecture](./2026-07-03-ux-2-architecture-handoff.md).
**When these land:** re-run the job-flow trace (method in the [audit](../plans/2026-07-03-ux-flow-audit.md))
to confirm every core job completes end-to-end — that closes the audit.

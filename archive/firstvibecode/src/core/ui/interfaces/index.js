/**
 * UI Interfaces Export
 * 
 * Exports all UI interfaces for easy importing
 */

const UIComponent = require('./UIComponent');
const UIRenderer = require('./UIRenderer');
const UIManager = require('./UIManager');

module.exports = {
  UIComponent,
  UIRenderer,
  UIManager
}; 